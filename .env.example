# PostgreSQL connection string — set in Render dashboard, never commit the real value
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# Mapbox public access token — get from mapbox.com/account/access-tokens
NEXT_PUBLIC_MAPBOX_TOKEN="pk.eyJ1IjoiLi4uIn0..."

# Absolute base URL of the deployed app — used by Apollo Client for SSR
# Local dev: http://localhost:3000
# Production: https://crashmap.io (or your .onrender.com URL until the domain is wired up)
NEXT_PUBLIC_APP_URL="https://crashmap.io"

# Sentry DSN — get from your Sentry project Settings > Client Keys (DSN)
# Safe to be public (it's already in your JS bundle); add to Render env vars too
NEXT_PUBLIC_SENTRY_DSN=""

# Sentry source-map upload — only needed in CI (GitHub Actions secrets)
# SENTRY_AUTH_TOKEN — create at https://sentry.io/orgredirect/organizations/:org/settings/auth-tokens/
# SENTRY_ORG — your Sentry org slug (visible in Settings > General)
# SENTRY_PROJECT — your Sentry project slug (visible in project Settings)
SENTRY_AUTH_TOKEN=""
SENTRY_ORG=""
SENTRY_PROJECT=""
